# If the dev host is the same as the prod host, then simply put the same
# host name under each section.

[prod]
prod.EXAMPLE.COM ansible_ssh_user=root

[stage]
prod.EXAMPLE.COM ansible_ssh_user=root

[dev]
dev.EXAMPLE.COM ansible_ssh_user=root

[multi:children]
prod
stage
dev